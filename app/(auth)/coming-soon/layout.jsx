export const metadata = {
  title: 'HMSTR Dashboard',
  description: ' is a popular dashboard template.',
}

const layout = ({ children }) => {
  return (
    <>
      {children}
    </>
  );
};

export default layout;